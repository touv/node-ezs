#!/usr/bin/env node
const prog = require('caporal');
const ezs = require('../lib');
const { version } = require('../package.json');

prog
  .version(version)
  .description('Run ezs server.')
  .action(() => {
      ezs.createCluster();
  });

prog.parse(process.argv);
